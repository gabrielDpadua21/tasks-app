<resources xmlns:tools="http://schemas.android.com/tools">
    <!-- Base application theme. -->
    <style name="Base.Theme.MyTasksApp" parent="Theme.Material3.DayNight.NoActionBar">
        <item name="android:windowBackground">
            @color/default_color
        </item>
    </style>

    <style name="Theme.MyTasksApp" parent="Base.Theme.MyTasksApp" />
</resources>